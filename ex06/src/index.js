const firstName = (firstName) => {
    var toUpper = firstName.toUpperCase();
    return toUpper;
}

const lastName = (lastName) => {
    var toLower = lastName.toLowerCase();
    return toLower;
}
exports.firstName = firstName;
exports.lastName = lastName;
